builds:
  build_1:
    environment:
      LAST_NAME=Johansen
    script:
      - bundle exec rspec
environment:
  - FIRST_NAME=Kasper
  - RUBY_VERSION=3.3.6
rvm: true
services:
  postgres:
    environment:
      MIDDLE_NAME=Nielsen
    image: postgres:9.6
