builds:
  build_1:
    environment:
      LAST_NAME=Johansen
    script:
      - bundle exec rspec
environment:
  - FIRST_NAME=Kasper
  - RUBY_VERSION=2.7.5
rvm: true
services:
  postgres:
    environment:
      MIDDLE_NAME=Nielsen
    image: postgres:9.6
