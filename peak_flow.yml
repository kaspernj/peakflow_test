environment:
  RUBY_VERSION: 2.5.7
rvm: true
services:
  myservice:
    auth:
      username: hitestprivatepeakflow
      password: $DOCKER_HUB_PASSWORD
    image: peakflow/private-test
script:
  - bundle exec rspec
