services:
  postgres:
    environment:
      POSTGRES_USER: "user"
      POSTGRES_PASSWORD: "password"
      POSTGRES_DB: "database"
    expose:
      - 5432
    image: postgres:9.6
script:
  - bash wait_for_postgres_container.sh
  - bundle exec rspec
